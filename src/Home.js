import HeroSection from "./components/HeroSection";

const HomeElement = document.createElement("div");
HomeElement.className = "home-page";

HomeElement.appendChild(HeroSection);

export default HomeElement;
